httpx[http2]>=0.27,<1
livekit-agents[silero,turn-detector]>=1.4,<2
prometheus_client>=0.20,<1
pydantic-settings>=2,<3
python-dotenv>=1,<2

# TEMP: required by agent/plugins/rag/ — delete with that subtree when
# Nusuk /chat/stream is fixed upstream.
pymilvus>=2.4,<3

